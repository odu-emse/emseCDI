module.exports = {
  mode: 'jit',
  purge: ['./packages/**/*.{js,ts,jsx,tsx}', './packages/renderer/index.html'],
    darkMode: false, // or 'media' or 'class'
    theme: {
    },
    variants: {
        extend: {},
    },
    plugins: [],
}
